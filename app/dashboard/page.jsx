'use client';

import Link from 'next/link';
import { useEffect, useState } from 'react';
import { Shell } from '@/components/site-shell';
import { Reveal } from '@/components/reveal';

const sources = [['Grid electricity', '61%'], ['Diesel generator', '24%'], ['Business travel', '10%'], ['Other', '5%']];

function Capture({ onStart, loading }) {
  const [drag, setDrag] = useState(false);
  const choose = () => document.getElementById('capture-input')?.click();
  return <div id="capture-stage" className={loading ? 'is-leaving' : ''}>
    <div className={`capture-box ${drag ? 'is-drag' : ''}`} role="button" tabIndex="0" aria-label="Capture or upload a bill" onClick={choose} onKeyDown={(event) => { if (event.key === 'Enter' || event.key === ' ') { event.preventDefault(); choose(); } }} onDragOver={(event) => { event.preventDefault(); setDrag(true); }} onDragLeave={() => setDrag(false)} onDrop={(event) => { event.preventDefault(); setDrag(false); if (event.dataTransfer.files.length) onStart(); }}>
      <div className="capture-icon"><svg viewBox="0 0 24 24" fill="none" stroke="#22C55E" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><path d="M4 8a2 2 0 012-2h1.2a1 1 0 00.86-.5l.9-1.5a1 1 0 01.86-.5h4.36a1 1 0 01.86.5l.9 1.5a1 1 0 00.86.5H18a2 2 0 012 2v9a2 2 0 01-2 2H6a2 2 0 01-2-2V8z"/><circle cx="12" cy="13" r="3.5"/></svg></div><h2>Capture or upload a bill</h2><p>An electricity bill, fuel receipt, or travel ticket &mdash; any photo works. Nothing leaves your browser in this preview.</p><input type="file" id="capture-input" accept="image/*" capture="environment" onChange={(event) => event.target.files?.length && onStart()} onClick={(event) => event.stopPropagation()} /><div className="capture-actions"><button type="button" className="btn-primary" onClick={(event) => { event.stopPropagation(); choose(); }}>Choose a photo</button><button type="button" className="btn-secondary" onClick={(event) => { event.stopPropagation(); onStart(); }}>Try a sample bill</button></div><p className="capture-hint">No file? &quot;Try a sample bill&quot; runs the dashboard with example data.</p>
    </div>{loading && <div className="capture-loading is-active"><span className="spinner"></span><span>Calculating footprint&hellip;</span></div>}
  </div>;
}

function DashboardView({ onReset }) {
  const [visible, setVisible] = useState(false);
  const [count, setCount] = useState(0);
  useEffect(() => { requestAnimationFrame(() => setVisible(true)); const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches; if (reduced) { setCount(859); return; } let frame; const start = performance.now(); const step = (now) => { const progress = Math.min((now - start) / 1100, 1); setCount(859 * (1 - Math.pow(1 - progress, 3))); if (progress < 1) frame = requestAnimationFrame(step); }; frame = requestAnimationFrame(step); return () => cancelAnimationFrame(frame); }, []);
  return <div id="dashboard-view" className={visible ? 'is-visible' : ''} style={{ display: 'block' }}><div className="dash-topbar"><span className="who"><span className="dash-dot live"></span> snap2green.scan / bhiwandi&#8209;unit&#8209;04</span><span>report ready</span></div><div className="dash-grid"><div className="dash-card dash-hero-stat"><div className="dash-label">kg CO&#8322;e / month</div><div><span className="dash-hero-num">{Math.round(count)}</span><span className="dash-hero-delta">&#8595; 6.2%</span></div><svg className="dash-spark" viewBox="0 0 300 80" preserveAspectRatio="none"><defs><linearGradient id="sparkGradient" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#22C55E" stopOpacity="0.35"/><stop offset="100%" stopColor="#22C55E" stopOpacity="0"/></linearGradient></defs><path className="fill" d="M0,55 L25,50 L50,58 L75,42 L100,46 L125,30 L150,36 L175,22 L200,28 L225,18 L250,24 L275,12 L300,16 L300,80 L0,80 Z"/><path className="line" d="M0,55 L25,50 L50,58 L75,42 L100,46 L125,30 L150,36 L175,22 L200,28 L225,18 L250,24 L275,12 L300,16"/></svg></div><div className="dash-card dash-gauge-card"><div className="dash-label" style={{ alignSelf: 'flex-start' }}>Data confidence</div><div className="dash-gauge"><svg viewBox="0 0 110 110" width="110" height="110"><circle cx="55" cy="55" r="48" fill="none" stroke="#26262A" strokeWidth="9"/><circle cx="55" cy="55" r="48" fill="none" stroke="#22C55E" strokeWidth="9" strokeLinecap="round" strokeDasharray="301.6" strokeDashoffset="11"/></svg><div className="dash-gauge-num"><span className="v">96.4%</span><span className="u">verified</span></div></div><p className="dash-gauge-cap">Matched to CEA grid data</p></div><div className="dash-card"><div className="dash-label">Active sources &middot; 4</div><div className="dash-list">{sources.map(([name, pct], index) => <div className={`dash-list-row ${index === 0 ? 'is-top' : ''}`} key={name}><div className="dash-list-top"><span className="name">{name}</span><span className="pct">{pct}</span></div><div className="dash-bar"><span style={{ width: pct }}></span></div></div>)}</div></div><div className="dash-card"><div className="dash-label">Scan log</div><div className="dash-log"><div className="dash-log-row"><span className="t">04:52</span><span>Electricity bill scanned &mdash; Aug cycle</span></div><div className="dash-log-row"><span className="t">03:18</span><span>Hotspot recalculated</span></div><div className="dash-log-row"><span className="t">01:46</span><span>Diesel receipt scanned</span></div><div className="dash-log-row flag"><span className="t">00:21</span><span>Spike flagged &mdash; generator hours up 18%</span></div></div></div><div className="dash-card"><div className="dash-label">Emission sources</div><div className="dash-stack-bar"><span style={{ width: '61%', background: '#16A34A' }}></span><span style={{ width: '24%', background: '#22C55E' }}></span><span style={{ width: '10%', background: '#4ADE80' }}></span><span style={{ width: '5%', background: '#26262A' }}></span></div><div className="dash-stack-legend"><span><i style={{ background: '#16A34A' }}></i>Electricity 61%</span><span><i style={{ background: '#22C55E' }}></i>Diesel 24%</span><span><i style={{ background: '#4ADE80' }}></i>Travel 10%</span><span><i style={{ background: '#26262A' }}></i>Other 5%</span></div></div><div className="dash-card"><div className="dash-label">Grid zone</div><div className="dash-location-name">Bhiwandi, MH</div><div className="dash-location-cap">Western region &middot; 0.716 kg CO&#8322;/kWh</div></div><div className="dash-card"><div className="dash-label">Report</div><div className="dash-report-row"><div className="dash-avatars"><span>Y</span><span>+1</span></div><span className="dash-report-last">generated 2m ago</span></div></div></div><div style={{ marginTop: 26, display: 'flex', gap: 14, flexWrap: 'wrap' }}><button type="button" className="btn-secondary" onClick={onReset}>Scan another bill</button><Link className="btn-primary" href="/contact">Get early access</Link></div></div>;
}

export default function DashboardPage() {
  const [processing, setProcessing] = useState(false);
  const [ready, setReady] = useState(false);
  const start = () => { if (processing || ready) return; setProcessing(true); window.setTimeout(() => { setProcessing(false); setReady(true); }, 1650); };
  const reset = () => { setReady(false); setProcessing(false); };
  return <Shell current="dashboard"><main><section className="page-hero"><Reveal><div className="wrap"><p className="eyebrow">Your footprint</p><h1 style={{ marginTop: 10 }}>See what one photo turns into.</h1><p className="lede">Capture a bill below to preview the dashboard Snap2Green builds from it &mdash; this runs entirely in your browser as a demo, with sample data standing in for a real scan.</p></div></Reveal></section><section style={{ paddingTop: 0 }}><Reveal><div className="wrap"><div className="dash-shell">{!ready && <Capture onStart={start} loading={processing} />}{ready && <DashboardView onReset={reset} />}</div></div></Reveal></section></main></Shell>;
}
